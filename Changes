foreach (var portion in paragraph.Portions)
{
    existingText = portion.Text;

    if (existingText.Contains("[UNDERLYING_BULLET_POINTS]"))
    {
        var bulletText = replaceText.ReplacePlaceholderWithValue("[UNDERLYING_BULLET_POINTS]", brochureData, strategyMdsCode, productCharacteristics.Where(bd => bd.IsBenchmark == false).FirstOrDefault());

        var bulletPoints = bulletText.Split(new[] { "\n", "\r\n" }, StringSplitOptions.RemoveEmptyEntries);
        paragraph.Portions.Clear(); // Remove the original portion

        foreach (var bullet in bulletPoints)
        {
            var newPara = new Paragraph();
            newPara.ParagraphFormat.Bullet.Type = BulletType.Symbol;
            newPara.ParagraphFormat.Bullet.Character = Convert.ToChar(8226); // •
            newPara.ParagraphFormat.Bullet.Font = new FontData("Arial");

            var newPortion = new Portion(bullet.Trim());
            newPara.Portions.Add(newPortion);

            textFrame.Paragraphs.Add(newPara);
        }

        break; // exit the portion loop once replaced
    }
    else
    {
        updatedText = replaceText.ReplacePlaceholderWithValue(existingText, brochureData, strategyMdsCode, productCharacteristics.Where(bd => bd.IsBenchmark == false).FirstOrDefault());
        portion.Text = updatedText;
    }
}